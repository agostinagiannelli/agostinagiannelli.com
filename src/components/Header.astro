---
import ThemeToggle from "./ThemeToggle.astro";

const ITEMS = [
  {
    title: "About",
    label: "about",
    url: "/",
  },
  {
    title: "Contact",
    label: "contact",
    url: "/contact",
  },
  {
    title: "Case Studies",
    label: "caseStudies",
    url: "/case-studies",
  },
];
---

<header
  class="fixed top-0 z-10 flex items-center justify-center w-full mx-auto mt-5"
>
  <nav
    class="flex items-center justify-center rounded text-slate-600 dark:text-slate-200"
  >
    {
      ITEMS.map(({ title, label, url }) => (
        <a
          class="relative block px-4 py-2 transition hover:text-purple-400 dark:hover:text-purple-400"
          aria-label={label}
          href={url}
        >
          {title}
        </a>
      ))
    }
    <ThemeToggle />
  </nav>
</header>

<style>
  nav {
    animation: nav-shadow 1s linear both;
    animation-timeline: scroll();
    animation-range: 0 100px;
  }

  @keyframes nav-shadow {
    to {
      @apply shadow-lg ring-1 backdrop-blur dark:bg-slate-800/90 bg-white/50 ring-white/10;
    }
  }
</style>
